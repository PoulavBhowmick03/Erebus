# v0.2.0 current-commit clean-install canary

**Date:** 2026-09-02  
**Source commit:** `a332f795d763a3375e3fdcda1845e7cbbdd4dfcf`  
**Wheels:** [run 33667032533](https://github.com/PoulavBhowmick03/Erebus/actions/runs/33667032533)  
**Canary:** [run 33667181818](https://github.com/PoulavBhowmick03/Erebus/actions/runs/33667181818)  
**Result:** Automated artifact and clean-install checks passed. The independent-person guide
review remains open.

## Artifact result

The workflow built the Linux x86-64 and macOS arm64 CLI wheels, both raw binaries, the
platform-independent SDK and MCP wheels, checksums, and the software bill of materials.
Each native CLI wheel was installed into a fresh environment and executed before collection.
The release-readiness gate and checksum collection passed. No release or tag was created.

## Clean-install matrix

| Environment | Python | Result |
| --- | --- | --- |
| Ubuntu | 3.11 | passed |
| Ubuntu | 3.13 | passed |
| macOS arm64 | 3.11 | passed |
| macOS arm64 | 3.13 | passed |

Each environment:

- verified the downloaded checksums;
- installed only the generated wheels into an empty virtual environment;
- imported `erebus`, `erebus_mcp`, and `erebus_cli`;
- located and ran the packaged `erebus-cli` without a Rust toolchain;
- started the packaged MCP server;
- exposed the Protocol 4 tool surface;
- called `doctor` through MCP against a deliberately unreachable local endpoint and received
  the structured read-only report.

## Boundary

This is reproducibility evidence from isolated GitHub-hosted runners. It does not satisfy the
roadmap requirement for a person outside the implementation path to read the guide, identify
unclear instructions, and report hidden prerequisites.

The runners emitted non-failing warnings about GitHub Actions moving JavaScript actions from
Node.js 20 to Node.js 24 and about the sparse canary checkout containing no dependency-lock
file for the setup cache. Neither warning changed the install or MCP result.
